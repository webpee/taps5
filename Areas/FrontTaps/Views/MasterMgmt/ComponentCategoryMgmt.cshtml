
@{
    ViewBag.Title = "ComponentCategoryMgmt";
    Layout = "~/Areas/FrontTaps/Views/Shared/_Layout.cshtml";
}

<div ng-controller="ComponentCategoryMgmtCtrl">
    <div class="row">
        <div class="col-lg-12">
            <div class="box">
                <div class="box-header">
                    <h2><i class="fa fa-edit"></i>Component Category Management</h2>
                    <div class="box-icon">
                        <a href="form-elements.html#" class="btn-minimize"><i class="fa fa-chevron-up"></i></a>
                    </div>
                </div>
                <div class="box-content">
                    <form role="form"
                          id="form"
                          name="form"
                          class="form-horizontal"
                          novalidate
                          ng-submit="formSubmit()">
                        <input type="hidden" value="{{model.id}}" ng-model="model.id" />
                        <div class="form-group">
                            <label class="control-label">Category Name :-</label>
                            <div class="input-group col-sm-4">
                                <select id="idCategory" name="idCategory" ng-model="model.idCategory" class="form-control" ng-required="true" ng-change="FetchComponentByCategory()">
                                    <option value="">-- Select Category --</option>
                                    <option ng-repeat="o in CategoryList" value="{{o.id}}">{{o.Name}}</option>
                                </select>
                            </div>
                        </div>
                        <div class="form-group">
                            <label class="control-label">Component Category Name :-</label>
                            <div class="input-group col-sm-4">
                                <input id="Name" type="text" maxlength="255" class="form-control" ng-required="true" ng-model="model.Name">
                            </div>
                        </div>
                        <div class="form-group">
                            <label class="control-label">Parent Component :-</label>
                            <div class="input-group col-sm-4">
                                <select id="idParent" name="idParent" ng-model="model.idParent" class="form-control">
                                    <option value="">-- Select Parent Component --</option>
                                    <option ng-repeat="o in Component" ng-show="model.idCategory==o.idCategory"  value="{{o.id}}">{{o.Name}}</option>
                                </select>
                            </div>
                        </div>
                        <div class="form-group">
                            <label class="control-label">Sequence :-</label>
                            <div class="input-group col-sm-4">
                                <input id="Seq" type="text" maxlength="10" class="form-control" numbers-only ng-model="model.Seq">
                            </div>
                        </div>
                        <br />
                        <div class="form-actions">
                            <button type="button" class="btn btn-primary" ng-disabled="!form.$valid" ng-click="CreateComponentCategory(model)">Save</button>
                            <button type="reset" class="btn" ng-click="Reset()">Cancel</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
        <div class="col-lg-12">
            <div class="box">
                <div class="box-header" data-original-title>
                    <h2><i class="fa fa-user"></i><span class="break"></span>Component Category Management List</h2>
                    <div class="box-icon">
                        <a href="table.html#" class="btn-minimize"><i class="fa fa-chevron-up"></i></a>
                    </div>
                </div>
                <div class="box-content">
                    <div class="col-lg-8">
                    </div>
                    <div class="col-lg-1">
                        <h4>Search :</h4>
                    </div>
                    <div class="col-lg-3">
                        <input id="Search" type="text" maxlength="50" class="form-control" ng-model="model.SearchText5" ng-change="BlankSearch(model.SearchText5)" ng-enter="filter(model.SearchText5)" ng-focus="filter(SearchText5)" ng-blur="filter(model.SearchText5)">
                    </div>
                    <br /><br />
                    @{ Html.RenderPartial("_ListComponentCategoryMgmt"); }
                </div>
            </div>
        </div>
    </div>
</div>

<script src="~/app/controllers/MasterMgmt/ComponentCategoryMgmt.js"></script>